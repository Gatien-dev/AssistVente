@model IEnumerable<AssistVente.Models.Produit>

@{
    ViewBag.Title = "Index";
    ViewBag.position = "Produits";
}



<div class="row">
    <div class="col-md-12">
        <div class="card strpied-tabled-with-hover">
            <div class="card-header ">
                <p class="pull-right">
                    <a href="@Url.Action("Create")" style="margin-right:5px;" class="btn btn-primary"><span class="fas fa-plus"></span> Nouveau Produit</a>
                </p>
                <h4 class="card-title">Produits vendus</h4>
                <p class="card-category">Enregistrez vos produits pour effectuer des ventes</p>
            </div>
            <div class="card-body table-full-width table-responsive">
                @if (Model.Count() <= 0)
                {
                    <div class="placeholder col-md-10">
                        <span>
                            Aucun produit enregistré pour l'instant
                        </span>
                        <hr />
                        <h5>
                            <a href="@Url.Action("Create")" class="btn btn-default"><i class="nc-icon nc-simple-add"></i> Nouveau Produit</a>
                        </h5>
                    </div>

                }
                else
                {
                    <table class="table table-hover table-striped datatable">
                        <thead>
                        <th>
                            @Html.DisplayNameFor(model => model.Nom)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PrixAchat)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PrixVente)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ALouer)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.StockDisponible)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.StockEnLocation)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.DureeDeLocationParDefaut)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Categorie.Name)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.DateCreation)
                        </th>
                        <th>Actions</th>
                        </thead>
                        <thead>

                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Nom)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.PrixAchat)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.PrixVente)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ALouer)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Index","stocklogs", new { id = item.ID })" style="margin-right:2px; color:#0094ff; text-decoration:underline;" title="Historique du stock" >
                                            
                                            @Html.DisplayFor(modelItem => item.StockDisponible)
                                        </a>

                                        
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.StockEnLocation)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DureeDeLocationParDefaut)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Categorie.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DateCreation)
                                    </td>
                                    @*<td>
                                            @Html.DisplayFor(modelItem => item.CreatorId)
                                        </td>*@
                                <td class="small">

                                    <a href="@Url.Action("Details", new { id = item.ID })" style="margin-right:2px;" title="Details" class="btn btn-primary"><span class="fas fa-list"></span></a>
                                    <a href="@Url.Action("Edit", new { id = item.ID })" style="margin-right:2px;" title="Modifier" class="btn btn-default"><i class="fas fa-edit"></i></a>
                                    <a href="@Url.Action("Ajustement", new { id = item.ID })" style="margin-right:2px;" title="Ajuster le stock" class="btn btn-default"><i class="fas fa-wrench"></i></a>
                                    <a href="@Url.Action("VentesProduitPeriode","ventes", new { idProduit = item.ID })" style="margin-right:2px;" title="Ventes sur periode" class="btn btn-default"><i class="fa fa-calendar"></i></a>
                                    <a href="@Url.Action("VentesProduit","ventes", new { idProduit = item.ID })" style="margin-right:2px;" title="Ventes de ce produit" class="btn btn-default"><i class="fas fa-shopping-cart"></i></a>
                                    <a href="@Url.Action("Delete", new { id = item.ID })" class="btn btn-danger"><span class="fas fa-trash-alt"></span></a>
                                </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>
    </div>

</div>